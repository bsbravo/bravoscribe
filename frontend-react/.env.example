# Production only — points directly at Azure APIM.
# Leave unset for local dev: Vite's dev server proxies /api to http://localhost:8080 (nginx gateway).
VITE_API_BASE_URL=https://[apim-name].azure-api.net
