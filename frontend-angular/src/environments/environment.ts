// Local development environment.
// Mirrors SYSTEM_SPEC.md §6.3 — nginx gateway on localhost:8080 in local dev.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080',
};
